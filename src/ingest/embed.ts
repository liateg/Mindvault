import type { PersistableChunk } from "./chunk.js";

/** Day 11: turn chunks into vectors. Not implemented. */
export async function embedChunks(_chunks: readonly PersistableChunk[]): Promise<void> {
  return Promise.resolve();
}
